package com.class20;

public class Repl {
		public static String makeCapital(String str) {
		 return str.toUpperCase();
		 str.substring(1, 3);
		}
		
		public static void main(String[]args) {
		Repl object1=new Repl();
		
		object1.makeCapital("test");
		
		}
	}

